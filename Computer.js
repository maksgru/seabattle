import User from './User.js';
export default class Computer extends User {
    constructor(name) {
        super(name);
    }
    // shoot(target) {
    //   super.shoot(target)
    // }
    isPositionShootable() {
        // getPositionMargins and compare with injured positions
        // return shootable positions
        // return false
    }

    getShootablePositions() {

    }
}
